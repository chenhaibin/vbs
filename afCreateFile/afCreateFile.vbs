
strFileName = "newfile.txt"
boolRandom = True

set oFso = createobject ("scripting.filesystemobject")

if ofso.fileexists (strnewfile) = true then
	wscript.echo Now & ": File Exists"
	wscript.quit
else
	
end if

set ofile = ofso.createtextfile (strfile)

